#!/usr/bin/env node

var {server} = require('../index');

const PORT = process.env.PORT || 4003;

const options = {
    port: PORT 
}

server.start(options, () => console.log("Server is running in http://localhost:4003"));